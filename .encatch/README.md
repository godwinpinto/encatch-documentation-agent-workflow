# .encatch

Encatch platform tooling for this repository.

| Path | Purpose |
| --- | --- |
| `workflow/` | Local Cursor agent — GitHub issue triage and doc fix PRs |

Future additions (e.g. feedback bridge config) can live alongside `workflow/` here.
